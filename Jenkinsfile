pipeline {
    agent any
    stages {
        stage('clone') {
            steps {
                echo "clone from git"
            }
        }
        stage('buid') {
            steps {
              echo "build by maven"
            }
        }
    }
}
